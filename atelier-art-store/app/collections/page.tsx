import Header from "../components/Header";import Footer from "../components/Footer";import Link from "next/link";import {products} from "../products";export default function Collections(){const cs=Array.from(new Set(products.map(p=>p.collection)));return <><Header/><main className="container py-16"><div className="text-center mb-14"><p className="text-[10px] uppercase tracking-[.2em] mb-4">Explore</p><h1 className="serif text-6xl">Collections</h1></div><div className="grid md:grid-cols-3 gap-6">{cs.map((c,i)=>{const p=products.find(x=>x.collection===c)!;return <Link href={"/shop?collection="+encodeURIComponent(c)} key={c} className="group"><div className="overflow-hidden aspect-[4/5]"><img src={p.image} className="w-full h-full object-cover art" alt={c}/></div><h2 className="serif text-2xl mt-4">{c}</h2><p className="text-[10px] uppercase tracking-widest text-[var(--muted)] mt-1">View collection →</p></Link>})}</div></main><Footer/></>}
