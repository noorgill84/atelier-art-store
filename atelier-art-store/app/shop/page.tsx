 "use client"; import Header from "../components/Header"; import Footer from "../components/Footer"; import ProductCard from "../components/ProductCard"; import {products} from "../products"; import {useMemo,useState} from "react";
export default function Shop(){const[q,setQ]=useState("");const[c,setC]=useState("All");const[sort,setSort]=useState("new");const collections=["All",...Array.from(new Set(products.map(p=>p.collection)))];const list=useMemo(()=>products.filter(p=>(c==="All"||p.collection===c)&&p.title.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==="price"?a.price-b.price:sort==="pricehi"?b.price-a.price:b.year-a.year),[q,c,sort]);return <><Header/><main className="container py-16"><div className="text-center mb-14"><p className="text-[10px] uppercase tracking-[.2em] mb-4">Original artwork</p><h1 className="serif text-6xl">Shop paintings</h1></div><div className="flex flex-col md:flex-row gap-3 mb-10"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search artwork..."/><select value={c} onChange={e=>setC(e.target.value)}>{collections.map(x=><option key={x}>{x}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)}><option value="new">Newest</option><option value="price">Price: low to high</option><option value="pricehi">Price: high to low</option></select></div><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-x-5 gap-y-12">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div></main><Footer/></>}
